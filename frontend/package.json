{
  "name": "palm-health-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Palm Health SPA - Vanilla JavaScript single-page application styled with Tailwind CSS.",
  "scripts": {
    "build:css": "tailwindcss -i src/css/input.css -o src/css/styles.css --minify",
    "watch:css": "tailwindcss -i src/css/input.css -o src/css/styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
