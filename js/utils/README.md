# Utils / Helpers / Constants

- `utils/` — pure utility functions (date formatting, validation helpers, debounce).
- `helpers/` — DOM helpers (element creation, event delegation, focus management).
- `constants/` — route paths, API endpoints, UI texts shared across modules.

Populated from Module 6 onward. Every function documented with JSDoc in English.
